version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - CF_TUNNEL_TOKEN=${CF_TUNNEL_TOKEN}
    restart: unless-stopped
    # The container will run a Python HTTP server and expose it via Cloudflare Tunnel
